<%= form_with(model: forex, local: true) do |form| %>
  <% if forex.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(forex.errors.count, "error") %> prohibited this forex from being saved:</h2>

      <ul>
      <% forex.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :currency %>
    <%= form.text_field :currency %>
  </div>

  <div class="field">
    <%= form.label :rate %>
    <%= form.text_field :rate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
